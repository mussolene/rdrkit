title = "rdrkit gitleaks configuration"

[extend]
useDefault = true

[allowlist]
description = "Public documentation URLs and checksums are allowed; secrets are not."
paths = [
  '''Cargo\.lock''',
]
